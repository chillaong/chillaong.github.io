name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 监听 main 分支的提交

permissions:
  contents: write

jobs:
  build-and-deploy:
    concurrency: ci-${{ github.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Install and Build 🔧
        run: |
          npm ci
          npm run build
        env:
          # 如果您的仓库名是 ava-portfolio，这里不需要改，Vite配置里的 base 会处理
          CI: true

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 默认打包目录
          branch: gh-pages # 发布分支
